# MoonEP semantic oracle, vendored verbatim from MoonshotAI/MoonEP @ 7745ffa
# (workspace checkout: workspace/changchen/andrewy/MoonEP).
#
# These files define the ground truth for the MoonEP-semantics layer0 arm:
# the production MoonEP planning kernel is itself tested bit-equal against
# planning_reference.py (MoonEP tests/test_planning.py), so plan equality
# against this oracle is the fidelity contract of the port. Vendored (not
# imported from the MoonEP checkout) so sweep capsules never depend on a
# sibling repo's state.
