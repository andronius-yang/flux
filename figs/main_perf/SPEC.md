# Main performance figure — aesthetic & architecture spec

Status: REV 2.1 (2026-09-02, postdoc review + label-style rulings — see §9). Data authority: `figure_src.csv` (+
`figure_src.md` provenance). Generator: `make_figure.py` (matplotlib, to be
written after this spec is approved). Every value marked **[knob]** is a
parameter in the generator's single `CONFIG` block — nothing aesthetic is
hard-coded outside it.

## 1. Purpose and high-level decisions (user rulings, 2026-09-01)

- Clean, aesthetic figure aimed specifically at presenting scientific results.
- **NSDI (USENIX) two-column format, spanning both columns** (`\begin{figure*}`):
  target width = 7.0 in (\textwidth); the PDF is placed at `\linewidth` so the
  exported figure is built at exactly its final physical size — fonts are
  therefore true points, never scaled.
- **Four subfigures in a 2x2 grid** — columns = topology **{4 nodes, 16 nodes}**,
  rows = model **{K2, Qwen}** (K2 top).
- **Three bar groups per subfigure** — byte budgets **1 MiB, 4 MiB, 16 MiB**.
- **Seven bars per group**: 6 baselines + 1 "Ours".
- **One legend for the entire figure, at the top.**
- **Vertical budget (postdoc, 2026-09-02): figure + caption <= 1/4 of the
  page's text area.** USENIX text block is 7.0 x 9.0 in, so 25% = 2.25 in;
  with a ~0.35 in two-line caption the figure PDF is **1.85 in tall**
  (20.6% on its own, ~24% with caption).
- Compact: minimal inter-subfigure margins, wide bars, short inter-group
  gaps, ceiling hugging the tallest bar (postdoc, 2026-09-02).
- **Speedup ratios on every bar** vs the NVSHMEM baseline, vertical text,
  bold, two decimals, Ours in red (postdoc, 2026-09-02) — §2.4.
- Programmatically generated by a single, readable python/matplotlib script;
  all aesthetic knobs in one place.

## 2. The seven bars

### 2.1 Composition and fixed order

Bar order is FIXED across every group and subfigure (identity never moves),
arranged roughly greatest -> smallest so the group reads as a descent, with
Ours rightmost. The order was derived from mean rank over the 12 plotted
groups (see §7):

| pos | system (legend name **[knob]**) | figure_src `row_id` |
|---|---|---|
| 1 | FAST+GEMM | `fast_gemm` |
| 2 | NVSHMEM A2AV+GEMM | `nvshmem_gemm` |
| 3 | MoonEP | `moonep` |
| 4 | EPLB | `eplb` |
| 5 | EPIC | `epic` |
| 6 | COMET | `comet` |
| 7 | **Ours** | best-of (§2.2) |

### 2.2 The "Ours" bar — best-of selection (data rule)

Per (subfigure, budget) group, Ours = min total_ms over the available ours
arms: `ours1_tokencomm` (comm only), `ours2_nooverlap` (placement+routing,
no overlap), `ours2_direct` (placement+routing on direct a2av, 16n only),
`ours12` (comm+placement+routing), `ours12_dispatch` (+ per-iteration expert
planning, no dispatch). The winner set **[knob: OURS_CANDIDATES]** is data,
not aesthetics. On the committed data the winners are:

- `ours12` in 10/12 groups;
- `ours2_direct` at **16n b1, both models** (7.22 K2 / 6.83 Qwen).

The figure does not label which variant won (the prose/caption can); the bar
is simply "Ours".

### 2.3 FAST truncation

FAST at 16n (183–227 ms) would flatten every other bar. Mechanism
**[knob: TRUNCATE]**:

- Per subfigure **column**, the y-limit is set from the tallest
  *non-truncated* bar: `ylim = ceil_nice(max_non_fast * HEADROOM)`,
  HEADROOM = 1.12 **[knob]**.
- Any bar exceeding the y-limit is drawn to the limit and visually broken:
  two short parallel diagonal white slashes across the bar near its top (the
  standard broken-bar glyph), and the **true value printed vertically inside
  the bar just below the glyph** (e.g. "193", normal weight, on a solid
  window in the bar's own color so the hatch never crosses the digits) —
  REV 2 moved it inside so the ceiling band above the axes stays empty.
  Slash geometry (rise, gap, linewidth) **[knob: BREAK_MARK]**.
- Truncation threshold can be forced per column with an absolute override
  **[knob: YLIM_OVERRIDE = {col: value | None}]** (default None = automatic).
- On the committed data truncation fires only for FAST at 16n; the rule is
  generic (any bar over the cap truncates the same way).

### 2.4 Speedup annotations (REV 2)

Every non-truncated bar carries its **speedup vs the NVSHMEM A2AV+GEMM
baseline** in the same group: `ratio = NVSHMEM_total_ms / bar_total_ms`
(> 1 = faster than NVSHMEM). **[knob: SPEEDUP]**

- Text `"{:.2f}×"` (two decimals + multiplication sign), rotated 90° reading
  bottom-to-top, centered on the bar.
- **Weight (REV 2.1)**: regular for baselines, **bold only for Ours** — the
  postdoc's "reduce strain" ruling **[knob: SPEEDUP.weight, ours_weight]**.
- **Ink (REV 2.1)**: primary black for every label, including the inside
  ones on dark fills; **Ours in red `#c1121f`** **[knob: SPEEDUP.ours_color]**.
  White text on the inside window is reserved for fills darker than OKLCH
  L 0.40 **[knob: dark_text_L]** — i.e. only when monochrome legibility is
  genuinely at risk; none of the current fills qualify, so every inside
  label is black (verified legible on the grayscale proof).
- **The reference bar carries a "1×" mark (REV 2.1)** **[knob: ref_label]**
  so readers see which bar the ratios are relative to; it obeys the same
  placement and weight rules as any baseline label.
- Exclusion: truncated bars (`skip_truncated`; they show their true value
  instead, §2.3). FAST therefore carries a ratio at 4n (not truncated) and
  its true value at 16n.
- **Placement rule `auto` (above-first)**: the label sits *above* the bar
  when it fits under the y-ceiling; otherwise it sits *inside* the bar,
  top-anchored, on a solid window in the bar's own color. This is what lets
  the ceiling hug the tallest bar (§3) while short bars still label into the
  empty space above them. `placement` may be forced to `above` or `inside`.
- Fit is decided from an estimated glyph width (0.62 em, bold sans), and the
  generator's audit (§8) then verifies with matplotlib's *rendered* extents
  that no label crosses an axes edge or another label.

## 3. Layout

```
+------------------------------------------------------------------+
|   [legend: 7 entries, single row, centered, no frame]            |
|        4 nodes                          16 nodes                 |
| +--------------------+  +--------------------+                   |
| | K2                 |  | K2                 |   <- in-axes tag   |
| |  ||||||| ||||||| … |  |                    |                   |
| +--------------------+  +--------------------+                   |
| +--------------------+  +--------------------+                   |
| | Qwen               |  | Qwen               |                   |
| +--------------------+  +--------------------+                   |
|      1 MiB  4 MiB  16 MiB   (x group labels, bottom row only)    |
+------------------------------------------------------------------+
```

- Figure size **[knob: FIG_W, FIG_H]** = 7.0 x **1.85 in** (REV 2; was 3.6).
- Grid: 2x2, `wspace` 0.10, `hspace` 0.14 **[knob]** (REV 2: compacted from
  0.14/0.16); margins left 0.052 / right 0.968 / top 0.80 / bottom 0.115.
- **Column titles** "4 nodes" / "16 nodes" **[knob: COL_TITLES]** above the top
  row, 7.5 pt bold, 2.5 pt pad.
- **Model tags** ("K2" / "Qwen") **[knob: ROW_TAGS, ROW_TAG_STYLE="right"]**
  as rotated labels on the right edge of the right column, 7 pt, secondary
  ink (the in-axes top-left style is kept as `ROW_TAG_STYLE="inside"`; it
  collides with the truncated FAST bar at 16n, which is why "right" is the
  default).
- **Y sharing** **[knob: SHAREY = "col"]**: the two subfigures in a column
  share y-limits (4n and 16n magnitudes differ ~4x, so columns must scale
  independently; K2/Qwen within a column are comparable, and shared limits
  make the vertical model comparison honest). All four axes keep their own
  y tick labels; 3 major ticks **[knob: N_YTICKS]**.
- **Ceiling** (REV 2, "the diagram is too empty"): `ylim = ceil_nice(1.03 x
  tallest kept bar)` **[knob: HEADROOM, NICE_STEPS]** with a fine nice-step
  (2 below 60, 5 below 150, 10 above) — on the committed data 46 ms (4n) and
  80 ms (16n), so the tallest bars reach ~95% of the panel. The labels that
  would need headroom go inside those bars (§2.4).
- X: three group positions labeled `1 MiB / 4 MiB / 16 MiB` **[knob:
  GROUP_LABELS]**, bottom row only; no x tick marks (`length=0`); x-limits
  padded by 0.47 slot **[knob: X_PAD]** so the outer groups sit close to the
  spines.
- Y axis label: **one** shared "Latency (ms)" **[knob: Y_LABEL]** on the
  figure's left edge (`fig.supylabel`), vertically centered over both rows
  (user ruling 2026-09-01: a single left-side label).
- X axis label: none by default **[knob: X_LABEL = None]** — the group tick
  labels are self-describing ("1 MiB" ...), and the *meaning* of the budget
  axis (per-rank pre-topk send budget) is the caption's job, per the usual
  systems-paper convention. Setting X_LABEL to a string draws a shared
  `fig.supxlabel`.

## 4. Marks (bars)

- Group width **0.90** of the unit slot **[knob: GROUP_WIDTH]** (REV 2: was
  0.84 — wider bars, shorter 1/4/16 MiB gaps); 7 bars with an inter-bar gap
  of **10%** of bar width **[knob: BAR_GAP_FRAC]** (was 18%) — still a visible
  gap, the print equivalent of the 2px surface-gap rule (adjacent fills never
  touch).
- Bar style: flat fill, **edge stroke 0.45 pt in primary ink** (`#0b0b0b`)
  **[knob: EDGE_LW]** on every bar — this is the contrast-relief channel for
  the lighter fills (sand/teal are sub-3:1 against white; the dark edge
  guarantees every bar's silhouette) and it crisps photocopy/grayscale.
- **Hatch channel** **[knob: HATCHES]**: per-system hatch list, default
  `["////", None, "\\\\", "..", "xx", None, None]`-style subtle patterns for
  print/grayscale robustness, hatch linewidth 0.4 pt, hatch color = edge ink.
  Default **ON** (NSDI is routinely printed grayscale); flip to all-`None`
  for a pure-color look.
- **Ours emphasis**: steel blue + edge stroke 0.7 pt **[knob: OURS_EDGE_LW]**
  + its speedup label in red (§2.4) — weight and ink, not a different geometry.
- Labels: every non-reference, non-truncated bar carries its speedup (§2.4);
  truncated bars carry their true value (§2.3). No other value labels.

## 5. Color

Categorical palette in **display order** (color follows the system, fixed
everywhere) **[knob: COLORS]**. USER RULING 2026-09-01: muted/academic
tones, no pink and no light purple — the original vibrant reference palette
was replaced by this muted set, chosen by enumerating orderings of a muted
candidate pool with the dataviz six-checks validator (Python twin; math
identical: OKLab dE x 100, Machado severity-1.0 CVD) against white
`#ffffff` and taking the best-margin passing order:

| pos | system | hex | note |
|---|---|---|---|
| 1 | FAST+GEMM | `#c0653f` | terracotta |
| 2 | NVSHMEM A2AV+GEMM | `#ddaa33` | sand |
| 3 | MoonEP | `#2f7a4d` | deep green |
| 4 | EPLB | `#44bb99` | teal |
| 5 | EPIC | `#9d4a4a` | wine |
| 6 | COMET | `#999933` | olive |
| 7 | **Ours** | `#4878b0` | steel blue, rightmost |

Validation results (adjacent pairs = the on-page bar adjacency): lightness
band PASS, chroma floor PASS, **CVD separation worst 16.1 (>= 8 target)
PASS**, **normal-vision floor worst 18.6 (>= 15) PASS**, contrast WARN on
sand/teal vs white — relieved by the mandatory 0.5 pt edge stroke + hatch
channel (§4). Cautionary datum: the "keep old hue families" assignment
(wine beside deep green) measured **dE 1.1 under deuteranopia** — muted
red-green neighbors at matched lightness are invisible to dichromats, so
re-run the validator after ANY color change; never re-order by eye.

Chrome ink **[knob: INK]**: primary `#0b0b0b` (edges, tick labels, axis
labels), secondary `#52514e` (in-axes model tags), muted `#898781`
(annotation values), gridline `#e1e0d9`, axis line `#c3c2b7`.

## 6. Typography & chrome

- Font **[knob: FONT_FAMILY]**: `["Helvetica", "Arial", "DejaVu Sans"]` — a
  clean sans for figure text inside a Times body (standard NSDI practice).
  All text through matplotlib rcParams; `pdf.fonttype = 42` (TrueType
  embedding, camera-ready requirement).
- Sizes **[knob: FONT_SIZES]** (true points at 7 in width; REV 2 compaction):
  legend 7, column titles 7.5 (bold), model tags 7, y-axis label 7,
  tick labels 6.5, group (x) labels 7, speedup/truncation annotations 5.8
  (bold for speedups). 5.8 pt is the floor — below it the ratios stop being
  legible in print; raising it costs ceiling headroom (§2.4).
- Grid: horizontal hairline gridlines only (0.4 pt, `#e1e0d9`), drawn *behind*
  bars (`axisbelow`), ~4–5 ticks per axis via `MaxNLocator(nbins=4)`
  **[knob: N_YTICKS]**. No vertical gridlines.
- Spines: top and right **removed**; left + bottom in axis ink, 0.6 pt.
- Legend: single row of 7 **[knob: LEGEND_NCOL]**, flush with the top edge
  **[knob: LEGEND_Y]**, no frame, handle = small rect swatch (carrying
  fill+hatch+edge), column spacing 0.9 **[knob]**. Falls to 4+3 two-row
  layout if 7 entries overset the width at final font size.
- Background: pure white, no axes face tint.

## 7. Data contract (generator input)

- Input: `figs/main_perf/figure_src.csv` (committed, `ec1a3ec`). Filter
  `budget_mib in {1, 4, 16}`, pivot on (`nodes`, `model`, `row_id`).
- Plotted value: `total_ms` (the corrected authoritative column — NOT
  `recorded_ms`).
- Bar order + best-of rule as §2; the mean-rank derivation that fixed the
  order: FAST 0.50, NVSHMEM 1.75, MoonEP 2.00, EPLB 3.42, EPIC 3.50, COMET
  3.83, Ours 6.00 (0 = largest, over the 12 plotted groups).
- The generator must fail loudly (assert) if a (group, system) cell is
  missing — no silent zero-height bars.

## 8. Output & workflow

- `python3 figs/main_perf/make_figure.py` -> `main_perf.pdf` (vector, the one
  used in the paper) + `main_perf.png` at 300 dpi (preview) **[knob: OUTPUTS]**.
  Deterministic: same CSV -> byte-comparable PDF (fixed SOURCE_DATE_EPOCH).
- Script structure: `CONFIG` dict at top (every knob above) -> `load()` ->
  `plot()` -> `save()`; no class machinery, ~150 lines, comments only where a
  knob's unit is non-obvious.
- Render-and-look checklist before committing any regeneration:
  1. no label collisions (truncation values vs legend vs column titles);
  2. bar gaps visible at 100% zoom and in a grayscale export;
  3. the truncated FAST bars read as "off the scale", not as data at the cap;
  4. legend order matches bar order left->right;
  5. fonts embedded (`pdffonts` shows no Type 3);
  6. label audit (REV 2): with matplotlib's rendered extents, no rotated
     label crosses an axes edge and no two labels overlap (the audit snippet
     lives in the commit that introduced §2.4; 72 labels, 0 problems on the
     committed data);
  7. the figure PDF is 1.85 in tall (`pdfinfo` / PNG 2100x555 at 300 dpi).

## 9. Revision log

- **REV 1 (2026-09-01)** — initial spec; user rulings: hatch ON, COMET
  spelling, single y-label, no x-title, muted palette (no pink/purple).
- **REV 2 (2026-09-02) — postdoc visual review**, each point and its
  resolution:
  1. *Figure incl. caption should be 1/4 of the page's vertical space (20–25%
     of the text area).* -> FIG_H 3.6 -> **1.85 in** (§1 arithmetic); fonts
     one step down (§6); legend flush to the top edge.
  2. *Compact; less margin between subfigures.* -> wspace/hspace 0.14/0.16 ->
     **0.10/0.14**, outer margins tightened, column-title pad 11 -> 2.5 pt
     (possible because the truncation labels moved inside the bars).
  3. *Wider bars, shorter gaps between 1/4/16 MiB groups.* -> GROUP_WIDTH
     0.84 -> **0.90**, BAR_GAP_FRAC 0.18 -> **0.10**, X_PAD 0.5 -> 0.47.
  4. *Reduce the vertical ceiling; the diagram looks empty.* -> HEADROOM 1.12
     -> **1.03** with a finer nice-step; tallest bars now reach ~95% of the
     panel (46 / 80 ms ceilings).
  5. *Add speedup ratios vs NVSHMEM on the bars (except truncated FAST),
     vertical bottom-to-top, bold, two decimals, Ours in red.* -> §2.4.
     **Conflict with (1)+(4) resolved by the above-first / inside-fallback
     placement**: labels only consume headroom where there is spare space
     above short bars; the tallest bars host their label on a solid window
     of their own color. Truncated FAST bars show their true value inside
     instead of a ratio.
- **REV 2.1 (2026-09-02) — label-style rulings after seeing REV 2:**
  inside labels black (white only below OKLCH L 0.40 — none today); "1×"
  mark on the NVSHMEM reference bars; speedups regular weight, Ours bold;
  break glyph confined to its own bar (BREAK_MARK.halfw 0.75 -> 0.52,
  clipped) — the last is a knob tweak, not a rule.
