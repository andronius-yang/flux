# SCHEMA rule 13 flip gate (2026-08-24): the NEW l01_slipstream (pinned) vs
# its bare-defaults twin — bare must equal pinned within noise, proving the
# gen-11 binary defaults reproduce the canonical config. Correctness ON
# (random payload), b8 + b64 (a273879 precedent: flip gates include b64).
platform: perlmutter
nodes: 8
shape: k2
families:
  - "trace:model=Kimi-K2;pools=livecodebench/execution;layer=5;sem=homog;dslots=64:32"
matrix_instance: "001"
modes: [isolated]
budgets_mib: [8, 64]
variants:
  - l01_slipstream
  - l01_slipstream_bare
iters: 10
warmup_iters: 5
sm_margin: 8
timeout_s: 2400
idle_timeout_s: 900
retries: 1
extra_env:
  FLUX_A2AV_RS_SPIN_LIMIT: "20000000"
notes: "rule-13 flip gate: bare==pinned on the new Slipstream (random payload)"
