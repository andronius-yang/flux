# P5 combined layer0+1 continuous-pass capsule, low tier (binary B).
# Arms: torch two-layer unfused, stock flux pairing (allgather+dense),
# optimized pairing (lb_union+F+E winners / hier+eager). One timed window
# per isolated iteration; routing/schedule computed once in-window by
# layer0; l1 index builders untimed + reported (l1_index_build_ms).
# Acceptance gate per capsule: sweeps/check_l01_identity.py against the
# sibling l0/l1 capsules of the SAME binary.
platform: perlmutter
nodes: 4
variants:
  - l01_torch
  - l01_allgather_dense
  - l01_lbunion_hier_eager
families:
  - "trace:pools=mmlu/high_school_world_history;layer=92;sem=homog"
budgets_mib: [1, 2, 4, 8, 16]
topk: 8
G: 128
H: 4096
chunk_bytes: 8192
ffn_hidden: 4096
dtype: bfloat16
iters: 10
warmup_iters: 5
sm_margin: 8
modes: [isolated]
matrix_instance: "001"
skip_correctness: false
timeout_s: 900
notes: "comm-only campaign P5 l01 blow fwd: combined continuous pass, binary B"
